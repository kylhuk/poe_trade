# Issues

- [task-4][backend] `.venv/bin/pytest tests/unit` fails with exit `127`: `/bin/sh: 1: .venv/bin/pytest: not found`.
- [task-4][frontend] `npm --prefix frontend run test`, `npm --prefix frontend run build`, and `npm --prefix frontend run test:inventory` each fail with exit `254`: `ENOENT ... frontend/package.json` not found.
- [task-4][CLI] All selected smoke commands from README/CI fail with exit `127` because `.venv/bin/python` is missing.
- [task-4][CI] `docker compose -f docker-compose.yml -f docker-compose.qa.yml --env-file .env.qa config` fails with exit `1`: missing `.env.qa`.
- [task-4][schema] `.venv/bin/poe-migrate --status --dry-run` fails with exit `127`: `.venv/bin/poe-migrate: not found`.
- [task-4][active-repo] Rebaseline on 2026-03-14T20:06:38.992774+00:00 in `/home/hal9000/docker/poe_trade` produced 0 failing commands; `task-4-failure-groups.json` contains empty groups across backend/frontend/schema/CI/CLI/ML.
- [task-6][schema/data] Gold marts are fully empty in active ClickHouse (`gold_*` tables all `rows=0`, `max(time_bucket)=1970-01-01`) despite populated PSAPI upstream sources; this drives current zero-row strategy backtests as `no_data`.
- [task-6][strategy/cx] `cx_market_making` has `no_data` at both mart and upstream levels because `raw_currency_exchange_hour` and `silver_cx_markets_hour` are empty.
- [task-6][product-contract] Current gold marts are coarse state/monitoring aggregates and do not provide candidate-grade fields (semantic identity, listing-level evidence, execution-ready diagnostics), leaving a `coarse_mart_gap` risk once freshness is restored.
- [task-6][runtime] Strategy `strategy.toml` metadata (`minima`, `min_sample_count`, `cooldown_minutes`, `requires_journal`) is not enforced by scanner/backtest runtime; this is a separate contract defect (`runtime_metadata_ignored`) that does not explain present zero-row outputs because source mart rows are already zero.
- [task-7][service-registry][high] `service_registry()` always includes one-shot `schema_migrator` and optional `account_stash_harvester`, causing dashboard/homepage `running/total` denominator distortion for core service health.
- [task-7][service-health-inference][high] `scanner_summary_payload()` reports `status: ok` whenever scanner rows exist, even when `lastRunAt` is stale; no stale/degraded state is inferred from age.
- [task-7][strategy-runtime][high] scanner runtime ignores strategy metadata enforcement for `min_sample_count`, `cooldown_minutes`, `requires_journal`, and minima/params blocks from `strategy.toml`.
- [task-7][alerts-identity][high] alert IDs are run-scoped (`concat(scanner_run_id, '-', item_or_market_key)`), so semantically identical opportunities receive different IDs on each rerun.
- [task-7][alerts-cooldown][high] duplicate-alert cooldown/novelty handling is absent in scanner alert emission, enabling repeated `new` alerts for stable opportunities.
- [task-5][api-contract] `/api/v1/ops/contract` advertises `auth_mode=bearer_operator_token_or_cookie_session`, but cookie-only ops requests return `401 auth_required`; contract text does not match enforced auth boundary.
- [task-5][dashboard] `/api/v1/ops/dashboard` `topOpportunities` are sourced from critical alert/messages payload instead of scanner recommendations, violating task-4A opportunity contract semantics.
- [task-5][scanner-api] `/api/v1/ops/scanner/recommendations` ignores sorting/filter query params (e.g. invalid `sort=not_a_field` still returns `200`) and does not expose contract-required fields (`semanticKey`, `searchHint`, `itemName`, `expectedHoldMinutes`, `liquidityScore`, `freshnessMinutes`, `goldCost`, `evidenceSnapshot`).
- [task-5][discoverability] Ops contract tabs and frontend navigation still omit a first-class scanner/opportunities surface; scanner recommendations are not discoverable as a dedicated product tab.
- [task-5][journal] Journal API remains absent (`/api/v1/journal/open` and `/api/v1/journal/positions` both return `404 route_not_found`), so `requires_journal` strategies cannot be supported through web API routes.
- [task-5][price-check] `price_check_payload()` hardcodes `comparables: []`, while `PriceCheckTab` presents comparable-items UI, creating a false implication of comparable-backed pricing evidence.
- [task-5][dashboard-internal] `dashboard/internal/app.js` fetches `/v1/ops/dashboard` (path mismatch vs `/api/v1/ops/dashboard`) and silently displays fallback telemetry on fetch failures.
- [task-11][ml/product-contract][high] ML outputs do not currently influence scanner recommendation ranking or opportunity payload projection: `strategy/scanner.py` populates recommendations only from strategy SQL source rows, and `api/ops.py::scanner_recommendations_payload()` reads only `poe_trade.scanner_recommendations`; this misses task-4A additive ML augmentation contract.
- [task-11][ml/opportunity-contract][high] Scanner recommendation/API surface still lacks task-4A-required additive fields (`semantic_key`, scanner/API `search_hint`, `item_name`, `expected_hold_minutes`, `liquidity_score`, `freshness_minutes`, `gold_cost`), so ML cannot safely attach contract-compliant ranking evidence.
- [task-11][ml/persistence][medium] Active model version persistence is currently provable only for `none` because `ml_model_registry_v1` has no promoted Mirage rows in the audited environment; non-`none` restart persistence lacks runtime evidence in this task scope.
- [task-10][credentials][high] Live credential-gated flow verification is blocked: no live `poeSessionId` bootstrap credential is available, and the candidate operator token from local `.env` is invalid for live (`GET /api/v1/ops/services` returns `401 auth_required` with bearer), so authenticated stash and allowlisted restart proofs cannot be executed.
- [task-13][frontend-dashboard-internal][high] `dashboard/internal/app.js` uses the wrong fetch path (`/v1/ops/dashboard` instead of `/api/v1/ops/dashboard`) and silently falls back to hardcoded telemetry on failure.
- [task-13][frontend-homepage-opportunities][high] `DashboardTab.tsx` renders "Top Opportunities" from `api.getMessages()` (alert-backed) instead of `api.getScannerRecommendations()` (scanner-backed).
- [task-13][frontend-opportunities-tab][high] There is no dedicated "Opportunities" or "Scanner" tab in the frontend navigation (`Index.tsx`).
- [task-13][frontend-comparables][medium] `PriceCheckTab.tsx` renders a comparables section, but the backend currently returns an empty array, creating a misleading UI.
- [task-13][frontend-stubbed-api][medium] Several API methods in `frontend/src/services/api.ts` are stubbed to return empty arrays or hardcoded objects.
- [task-13][frontend-unsupported-paths][low] "Session" and "Diagnostics" tabs in `AnalyticsTab.tsx` are explicitly marked as `feature_unavailable`.
- [task-9][live-frontend-cors][high] The live frontend at `https://poe.lama-lan.ch` is blocked by CORS policy when attempting to fetch data from `https://api.poe.lama-lan.ch`. The API calls for services, session, and messages fail with `net::ERR_FAILED`.
- [task-14][resolved][scanner-api][high] `/api/v1/ops/scanner/recommendations` now rejects unsupported `sort` values with `400 invalid_input`, honors `limit|min_confidence|league|strategy_id`, and returns additive opportunity-contract fields (`semanticKey`, `searchHint`, `itemName`, `expectedHoldMinutes`, `liquidityScore`, `freshnessMinutes`, `goldCost`, `evidenceSnapshot`) alongside legacy keys.
- [task-14][resolved][dashboard][high] `/api/v1/ops/dashboard` `topOpportunities` now projects scanner recommendations rather than critical message rows.
- [task-14][resolved][discoverability][medium] `/api/v1/ops/contract` tabs now advertise `opportunities` as a first-class product surface for frontend navigation.
- [task-16][qa-browser][high] `npx --prefix frontend playwright test` remains blocked by runtime/tooling errors (`bun:` ESM loader protocol, Vitest matcher symbol redefinition, JSON import attribute requirement); classify as `blocked_by_browser/runtime_tooling`.
- [task-16][live-parity][high] Live parity remains blocked by CORS for `https://poe.lama-lan.ch` -> `https://api.poe.lama-lan.ch`; origin probe still shows no `Access-Control-Allow-Origin`; classify as `blocked_by_live_CORS`.
- [task-16][credentials][high] Live credential-gated auth/stash/restart validation remains blocked: no live session bootstrap credential available and `.env` bearer token receives `401 auth_invalid`; classify as `blocked_by_credentials`.
- [task-16][ml-integration][medium] ML-to-scanner recommendation contract integration remains pending (`scanner_ml_influence_present=false` from task 11); classify as `fixed_later`.
- [task-16][env-diff][low] QA trusted-origin bypass versus live bearer enforcement remains an intentional target difference; classify as `intentional_environment_difference`.
