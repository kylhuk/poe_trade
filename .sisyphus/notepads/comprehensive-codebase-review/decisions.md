# Decisions

- Established a hard live mutation allowlist limited to restart verbs on `market_harvester`, `account_stash_harvester`, `scanner_worker`, and `ml_trainer` via `POST /api/v1/actions/services/{service_id}/restart`; all other live POST mutations are forbidden for this review track.
- Split execution policy into three explicit modes: `qa-only` (deterministic QA mutation allowed), `live-readonly` (GET/OPTIONS only), and `live-allowlisted-mutation` (restart-only with evidence and approval sequence).
- Treated crawled public stash data and canonical persisted history as immutable for review execution, forbidding delete/overwrite/replay-reset/truncate style actions at both data and control planes.
- Recorded missing requested plan path `.sisyphus/plans/comprehensive-codebase-review.md` and used orchestrator task packet plus repo state as authoritative fallback inputs for task-1 evidence.
- Correction: task-1 audit contract now treats `.sisyphus/plans/comprehensive-codebase-review.md` (absolute `/home/hal9000/docker/poe_trade_comprehensive-codebase-review/.sisyphus/plans/comprehensive-codebase-review.md`) as available and authoritative, with orchestrator packet and repo code state retained as co-authoritative inputs.
- Task-2 coverage inventory will treat `frontend/src/` as a required maintained surface with status `missing` (not silently excluded), and classify it as `untested` until source and package metadata are restored.
- Task-2 inventory will use owner lanes (backend-platform, backend-api, ingestion-runtime, data-platform, ml-platform, qa-automation, frontend-product, docs-ops, release-engineering, strategy-research, ops-platform) as subsystem ownership placeholders for downstream tasks 5-13.
- Exclusion accounting is restricted to generated/vendor/build/evidence/lockfile classes (for example `frontend/node_modules`, `frontend/dist`, Playwright outputs, `artifacts/`, `.sisyphus/evidence/`, and lockfiles) and explicitly keeps maintained source/docs/schema/tests in scope.
- Pinned the command matrix for QA and Live targets: QA uses port `18080` (API) and `4173` (Frontend), while Live uses `https://api.poe.lama-lan.ch` (Remote) or port `8080` (Local API).
- Mandated the use of `VITE_API_BASE_URL` for all frontend environment overrides to prevent silent fallthrough to the live API during QA or local development.
- Explicitly distinguished live targets in the command matrix: public UI (`https://poe.lama-lan.ch`), public unauthenticated API (`/healthz`), protected readonly API (`/api/v1/ops/*`), and allowlisted mutation API (`/api/v1/actions/*`).
- Adopted `semantic_key` as the canonical stable opportunity identity and explicitly demoted run-scoped `alert_id` to alert lifecycle state only; downstream UI/API/runtime work must key opportunity continuity on `semantic_key` (or deterministic `opportunity_id` derived from it).
- Locked product-flow rule for task 4A and downstream tasks: homepage top opportunities must come from scanner recommendations, while alerts/messages remain diagnostics/attention artifacts and cannot be treated as primary opportunities.
- Standardized contract-required recommendation fields across UI/API/runtime/SQL/observability/ML and marked missing fields as additive-only follow-up work (`semantic_key`, `search_hint`, `item_name`, `expected_hold_minutes`, `liquidity_score`, `freshness_minutes`, `gold_cost`).
- Chose additive compatibility posture for all proposed evolution: keep existing scanner fields (`item_or_market_key`, `expected_hold_time`, current API keys) during transition, add new fields/routes/views first, and defer any incompatible removals/renames to a later explicitly staged migration.
- Declared current placeholders/non-goals for this phase: no dedicated opportunities tab yet, no journal API endpoints yet, analytics diagnostics/session tabs remain unavailable placeholders, and price-check comparables remain empty until a real comparable source is added.
- Task-2 evidence is re-baselined to the active repo root (`/home/hal9000/docker/poe_trade`) and no longer references alternate-worktree paths.
- Maintained-scope decision update: `frontend/src/` and `frontend/package.json` are classified `present` and included in subsystem inventory ownership/check mapping.
- Exclusion decision update: keep only generated/vendor/build/evidence/lockfile classes excluded, explicitly including active `frontend/test-results/` and `frontend/package-lock.json`, while preserving maintained source/docs/tests/schema/workflows in scope.
